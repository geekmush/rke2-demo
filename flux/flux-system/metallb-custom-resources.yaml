apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: metallb-custom-resources
  namespace: flux-system
spec:
  dependsOn:
    - name: metallb
  interval: 10m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: apps/metallb-custom-resources
  prune: true
  wait: true
  timeout: 5m0s
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  suspend: false
